import { TurnData, TurnResponse } from "../../PokerTurn";
import IPokerRaise from "./IPokerRaise";

export default class TexasHoldemRaise implements IPokerRaise {
    execute(data: TurnData): TurnResponse {
        
    }
}